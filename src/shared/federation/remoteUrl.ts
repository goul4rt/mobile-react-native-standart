/**
 * The published version the stats tab loads. Empty means "use the screen in this
 * bundle". Every `npm run deploy:*` prints a fresh URL.
 */
export const REMOTE_URL = 'https://aroldogooulart-78-questiona-questiona-questoes-648f3b785-ze.zephyrcloud.app';
